TASK_NM="${taskNo}"; STRT_CMD="${service.start}";IFS=","; if [ -n "$STRT_CMD" ]; then for sc in $STRT_CMD; do eval "$sc" 1>&2 2>/dev/null; rc=$?; if [ $rc -ne 0 ]; then echo "ERROR : START COMMAND not able to execute, Code : $rc"; exit 1; fi; done; fi; if [ ! -d "$TASK_NM/backup" ]; then rm -rf ../$TASK_NM; fi; exit 0;